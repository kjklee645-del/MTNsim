# MTNsim 제품 방향 및 개발 로드맵

작성일: 2026-03-09

## 1. 제품 정의

MTNsim의 목표는 단순한 교통소음 계산기가 아니라, 개별 차량의 움직임과 도로 주변 3D 환경을 따라 초 단위 소음장을 재현하는 시뮬레이션 플랫폼이다.

핵심 가치는 다음 3가지다.

1. 개별 차량 기반
   - 차종, 속도, 가감속, 차로변경, 혼잡, 정지-출발 같은 이벤트가 소음에 어떻게 반영되는지 직접 계산한다.
2. 3D 환경 보정
   - 도로뿐 아니라 건물, 방음벽, 절개지, 옹벽, 지면재질, 반사/회절 영역까지 고려한다.
3. 실측 연계
   - 현장 센서 데이터로 모델을 보정해서 실제와 맞는 예측 모델로 발전시킨다.

이 방향은 현재 프로토타입의 개별 차량 추적, POI/격자 갱신, GPU 가속 구조와 맞고, 특허의 미세시간 단위 계산 및 지형지물 기반 보정값 산출 축과도 일치한다.

## 2. 권장 아키텍처

아키텍처는 아래 7개 엔진으로 분리하는 것이 적절하다.

1. Input Layer
   - GIS, CAD, SUMO 네트워크, 차량 시나리오, 실측 소음계 데이터를 읽는다.
2. Traffic Engine
   - 차량 궤적 생성과 상태 관리를 담당한다.
   - 초기에는 SUMO 연동, 이후에는 실측 궤적 재생 모드와 정책 시나리오 모드를 분리한다.
3. 3D Scene Engine
   - 지형, 건물, 방음벽, 재질, 수음점, 분석격자를 관리한다.
   - 특허의 3D 공간 생성부에 해당한다.
4. Emission Engine
   - 차량별 방사소음을 계산한다.
   - 입력: 차종, 속도, 가감속, 차선상태, 노면, EV/내연기관 구분
5. Propagation / Correction Engine
   - 자유음장 감쇠, 차폐, 회절, 반사, 지면효과, 재질 보정, 기상 보정을 담당한다.
6. Noise Field Engine
   - 격자와 수음점의 시간별 소음장을 계산한다.
   - 출력: 2D/3D 소음맵, POI 시계열, 차량별 기여도, 시간대별 통계량
7. Calibration Engine
   - 실측값으로 보정한다.
   - 오차맵 생성, 구간별 보정계수 추정, 재질/반사/차폐 계수 역추정, 신뢰도 점수 출력

## 3. 개발 우선순위

지금 당장 필요한 것은 기능 추가보다 구조 재편이다. 현재 스크립트는 실험에는 빠르지만 상용화에는 불리하다.

### 3.1 1단계: 커널 정리

목표는 재현 가능한 연구엔진 구축이다.

- SUMO 연동부 분리
- 소음 계산부 분리
- 격자/POI/수음점 데이터 모델 분리
- 실험 파라미터를 코드 밖 설정 파일로 이동
- 결과를 XML 대신 CSV, Parquet, SQLite 같은 구조화 포맷으로 저장

권장 폴더 구조:

- `src/traffic`
- `src/acoustics/emission`
- `src/acoustics/propagation`
- `src/scene`
- `src/calibration`
- `src/io`
- `src/visualization`
- `tests`

### 3.2 2단계: 계산 정확도 확보

- 현재 단순 식 기반 모델을 모듈화
- 수음점/격자별 검증 케이스 구축
- 실측과 비교하는 오차 분석 루틴 추가
- 반사/회절 없는 baseline과 있는 advanced 모드 분리

### 3.3 3단계: 3D 보정엔진 구축

특허 축이 실제 제품에 반영되는 단계다.

- 공간격자 생성
- 지형지물 중첩 판정
- 재질별 보정값 산출
- 반사/회절 영역 판정
- 소음격자와 공간격자 매핑

### 3.4 4단계: 제품화 레이어

- 프로젝트 파일 포맷
- 시나리오 비교 UI
- 소음맵 렌더러
- 보고서 생성
- GIS/CAD import/export
- 배치 실행과 결과 관리

## 4. 제품 기능 패키지

### 4.1 Research Edition

- SUMO 기반 시뮬레이션
- 수음점 시계열
- 격자 소음맵
- 속도/차선변경/교통량 실험
- GPU 계산

### 4.2 Professional Edition

- 3D 지형지물
- 방음벽/건물/절개지 반영
- 재질기반 반사/회절 보정
- 실측 데이터 보정
- 규제 기준별 결과 산출

### 4.3 Digital Twin Edition

- 실시간 교통 데이터 수집
- 센서 연동
- 운영 시나리오 비교
- 알람/이상 탐지
- 정책 적용 전후 효과 분석

## 5. MVP 범위

첫 상용 후보 버전은 아래 정도가 현실적이다.

- SUMO 연동
- 차종/속도 기반 차량별 방사소음
- 2.5D 지형 + 건물/방음벽 차폐
- 수음점 시계열 계산
- 격자 소음지도
- 실측 센서 3~10개 지점 보정
- 시나리오 비교 2개 이상
- 자동 보고서 PDF

이 정도면 논문/특허용 프로토타입을 넘어 기관 과제 시연 가능한 제품이 된다.

## 6. 기술 리스크

### 6.1 정확도

지금 식은 빠르지만 물리모델이 단순하다. 정확도 검증 체계 없이 기능만 늘리면 제품 신뢰성이 무너진다.

### 6.2 성능

차량 수 x 격자 수 x 시간스텝이 커지면 계산량이 급증한다. GPU는 선택이 아니라 필수다.

### 6.3 데이터

현장 데이터 품질이 낮으면 보정엔진이 흔들린다. 센서 메타데이터와 동기화 체계가 필요하다.

### 6.4 제품 구조

단일 스크립트 구조는 실험에는 좋지만 확장에는 취약하다. 지금 모듈화를 하지 않으면 나중에 다시 갈아엎게 될 가능성이 높다.

## 7. 2년 로드맵

### 0~3개월

- 코드 모듈화
- 실험 재현 구조 정리
- 결과 저장체계 정비
- 기본 테스트 구축

### 4~8개월

- Emission Engine 정식화
- 수음점/격자 엔진 분리
- baseline 정확도 검증
- GPU 파이프라인 정리

### 9~14개월

- 3D 지형지물/재질/차폐/회절 모듈
- 공간격자-소음격자 보정 엔진
- 특허 내용 구현화

### 15~20개월

- 실측 보정 엔진
- 시나리오 비교 UI
- 자동 보고서
- 과제/기관 시연 버전

### 21~24개월

- 실시간 데이터 연동
- 배치/서버 실행
- 상용 배포 구조 정리

## 8. 당장 해야 할 다음 작업

1. 현재 코드를 제품 아키텍처 기준 패키지 구조로 재설계
2. 소음 계산 커널과 SUMO 연동부를 분리
3. 특허 내용을 기능 명세로 변환
   - 3D 공간 생성부
   - 보정값 산출부
   - 소음영역 생성부
   - 소음도 보정부

## 9. 핵심 정리

MTNsim은 정적 소음지도 소프트웨어보다는, 개별 차량 이벤트를 시간축으로 재현하고 3D 지형지물 및 실측 데이터로 보정하는 교통소음 디지털 트윈 플랫폼으로 가는 것이 가장 경쟁력이 높다.
